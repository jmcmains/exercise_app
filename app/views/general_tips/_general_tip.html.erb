<tr>
	<td class="exercise <%= general_tip.id %>" id="<%= general_tip.id %>">
		<%= link_to "+", expand_general_tip_path(general_tip), :remote => true, :class => "expand", :id => "bu#{general_tip.id}" %></div>
		<%= link_to general_tip.name, expand_general_tip_path(general_tip), :remote => true, :class => "text", :id => "txt#{general_tip.id}"  %></div>
		<div class="data" id="ex<%= general_tip.id %>" style="display:none">
			<table class="allinfo">
				<tr>
					<td>
						<%= general_tip.content %>
					</td>
				</tr>
			</table>
		</div>
	</td>
</tr>
